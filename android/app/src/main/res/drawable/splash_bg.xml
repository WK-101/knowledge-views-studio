<?xml version="1.0" encoding="utf-8"?>
<!-- Instant branded launch window shown before Compose draws its first frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/ic_launcher_background" />
    <item android:gravity="center" android:width="180dp" android:height="180dp" android:drawable="@drawable/ic_launcher_foreground" />
</layer-list>
