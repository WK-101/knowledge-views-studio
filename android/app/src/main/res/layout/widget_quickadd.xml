<?xml version="1.0" encoding="utf-8"?>
<!-- R68 — a genuinely small 1×1 capture button. The root is TRANSPARENT (no full-bleed background), so
     however large a cell the launcher allocates, only the compact round ＋ tile paints — it never looks
     like a big 2×4 block. The whole cell stays tappable and opens the quick-capture popup. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="4dp">

    <TextView
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:gravity="center"
        android:background="@drawable/widget_add_bg"
        android:contentDescription="Add task"
        android:text="＋"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:textStyle="bold" />
</FrameLayout>
