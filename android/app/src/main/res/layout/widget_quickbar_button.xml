<?xml version="1.0" encoding="utf-8"?>
<!-- One Quick-bar action tile: an icon (drawn bitmap) with an optional short label under it.
     Added into a row at runtime; the whole tile carries the action's PendingIntent. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/qb_btn_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingLeft="6dp"
    android:paddingRight="6dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <ImageView
        android:id="@+id/qb_icon"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="fitCenter"
        android:importantForAccessibility="yes" />

    <TextView
        android:id="@+id/qb_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:text=""
        android:textColor="@color/w_text_secondary"
        android:textSize="10sp" />
</LinearLayout>
