<?xml version="1.0" encoding="utf-8"?>
<!-- One task row inside an Eisenhower quadrant list. A check-circle (coloured to the quadrant) that
     ticks the task off in place + the task title; tapping the title opens that task. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/mx_i_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="3dp"
    android:paddingBottom="3dp">

    <ImageView
        android:id="@+id/mx_i_check"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="7dp"
        android:scaleType="fitCenter"
        android:contentDescription="Complete task"
        android:importantForAccessibility="yes" />

    <TextView
        android:id="@+id/mx_i_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="Task"
        android:textColor="@color/w_text_primary"
        android:textSize="12sp" />
</LinearLayout>
